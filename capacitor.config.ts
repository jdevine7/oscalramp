import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.oscalramp.app',
  appName: 'oscalRamp',
  webDir: 'dist/oscal-ramp',
  bundledWebRuntime: false
};

export default config;
